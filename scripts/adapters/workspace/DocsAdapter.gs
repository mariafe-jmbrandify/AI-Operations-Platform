/**
 * DocsAdapter.gs
 * Adapter for Google Docs integration
 */

class DocsAdapter extends BaseAdapter {
  constructor(config = {}) {
    super(config);
  }

  initialize() {
    super.initialize();
  }
}
