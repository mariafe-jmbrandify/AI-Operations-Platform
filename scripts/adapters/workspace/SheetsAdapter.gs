/**
 * SheetsAdapter.gs
 * Adapter for Google Sheets integration
 */

class SheetsAdapter extends BaseAdapter {
  constructor(config = {}) {
    super(config);
  }

  initialize() {
    super.initialize();
  }
}
