/**
 * CalendarAdapter.gs
 * Adapter for Google Calendar integration
 */

class CalendarAdapter extends BaseAdapter {
  constructor(config = {}) {
    super(config);
  }

  initialize() {
    super.initialize();
  }
}
