/**
 * GmailAdapter.gs
 * Adapter for Gmail integration
 */

class GmailAdapter extends BaseAdapter {
  constructor(config = {}) {
    super(config);
  }

  initialize() {
    super.initialize();
  }
}
